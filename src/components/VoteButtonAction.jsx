import React from 'react';
import { MdThumbDown, MdThumbUp } from 'react-icons/md';
import PropTypes from 'prop-types';

function VoteButtonAction({ type, count, onButtonClicked }) {
  const isVoted = count !== 0;
}
